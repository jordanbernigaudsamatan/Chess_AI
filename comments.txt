pass a boolean to the function play moves to say if its a truly played move so that we can update the board pgn with this move and not each time the function is done
to compute legal_moves

write in a config (json?) file a matching between tuple coordinates and board coordinates (ex: "a1") and pass it as dict to clas chess_board
    --> can bes usefull for move2uci and readuci chess_board methods

write Class names without underscore and with caps at first position of words which compose classes (!Done!)